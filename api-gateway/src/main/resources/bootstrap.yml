spring:
  application:
    name: api-gateway
  cloud:
    config:
      discovery:
        enabled: true
        service-id: CONFIG
      profile: dev
  redis:
      port: 6379
      host: 192.168.99.100
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
